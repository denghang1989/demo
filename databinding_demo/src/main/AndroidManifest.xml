<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.databinding_demo"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".findview.SimpleDataBindingActivity"/>
        <activity android:name=".findview.SimpleDataBinding02Activity"/>
        <activity android:name=".simple_data.SimpleDataActivity"/>
        <activity android:name=".simple_image.SimpleImageActivity"/>
        <activity android:name=".simple_image_hold_error.HolderImageActivity"/>
    </application>

</manifest>